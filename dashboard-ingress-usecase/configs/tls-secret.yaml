apiVersion: v1
kind: Secret
metadata:
    name: tls-secret
    namespace: default

# TLS TYPE SECRET
type: kubernetes.io/tls

# ENCODED CERTIFICATES
data:
    tls.crt: <base64 encoded cert>
    tls.key: <base64 encoded key>